# Pinned to the known-good Python 3.12.10 venv (direct deps only, so the
# Mac/Linux installs don't inherit Windows-only transitive wheels).
asf_search==6.7.3
geopandas==1.1.3
shapely==2.1.2
rasterio==1.5.0
numpy==2.4.6
pandas==3.0.3
tkcalendar==1.6.1
psutil==7.2.2
